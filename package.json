{
  "private": true,
  "version": "0.0.0",
  "main": "gulpfile.js",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-changed": "^3.2.0",
    "gulp-image-resize": "^0.13.0",
    "gulp-imagemin": "^4.1.0",
    "imagemin-pngcrush": "^5.1.0"
  },
  "dependencies": {
    "bootstrap": "^4.5.2",
    "gulp-concat": "^2.6.1",
    "gulp-rename": "^1.4.0",
    "gulp-uglify": "^3.0.1"
  },
  "scripts": {
    "build": "npm install && bundle exec jekyll build",
    "serve": "npm install && bundle exec jekyll serve"
  }
}
